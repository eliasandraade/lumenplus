# Baseline de políticas oficiais — verificado em 2026-08-06

**Escopo:** requisitos de publicação nas lojas (Apple App Store, Google Play) e da toolchain
oficial (Expo / EAS) aplicáveis ao **Lumen+** — app de comunidade católica, Expo SDK 52 /
React Native 0.76, com contas de usuário, conteúdo gerado por usuário (posts em canais visíveis a
outros membros) e uso de câmera/fotos. **Sem compras no app, sem anúncios, sem tracking.**

**Regra de evidência deste documento:**

- Toda linha marcada `VERIFICADO` foi lida na URL oficial indicada, na data indicada.
- Fontes admitidas: `developer.apple.com`, App Store Review Guidelines, App Store Connect Help,
  `support.google.com/googleplay/android-developer`, `play.google.com/about/developer-content-policy`,
  `developer.android.com`, `docs.expo.dev`, `expo.dev/changelog`.
- **Nenhum blog, Medium, StackOverflow, YouTube ou fórum foi usado.**
- Linhas marcadas `NÃO VERIFICADO` **não podem sustentar nenhuma conclusão de conformidade** e
  estão consolidadas na seção final.

**Contagem:** 103 requisitos `VERIFICADO` · 27 itens únicos `NÃO VERIFICADO`.

**Data da consulta em todas as linhas abaixo: 2026-08-06.**

---

## 1. Apple — App Store Review Guidelines e App Store Connect

| Requisito | Aplicável ao Lumen+? | O que a fonte diz | Fonte oficial (URL) | Estado |
|---|---|---|---|---|
| **G1.2 UGC** — filtro de material censurável antes da publicação | **Sim** | Apps com UGC devem incluir "A method for filtering objectionable material from being posted to the app". Pode ser automatizado e/ou moderação pré-publicação, mas precisa existir e ser demonstrável ao App Review. | https://developer.apple.com/app-store/review/guidelines/#user-generated-content | VERIFICADO |
| **G1.2 UGC** — mecanismo de denúncia + resposta tempestiva | **Sim** | "A mechanism to report offensive content and timely responses to concerns". Exige **duas** coisas: o fluxo de denúncia no app **e** um processo operacional de resposta. UI sozinha não basta. | https://developer.apple.com/app-store/review/guidelines/#user-generated-content | VERIFICADO |
| **G1.2 UGC** — capacidade de bloquear usuários abusivos | **Sim** | "The ability to block abusive users from the service". Bloqueio acionável pelo próprio usuário (user-to-user), não apenas ban administrativo no backend. | https://developer.apple.com/app-store/review/guidelines/#user-generated-content | VERIFICADO |
| **G1.2 UGC** — informação de contato publicada | **Sim** | "Published contact information so users can easily reach you". Na prática: Support URL na ficha da App Store + contato acessível a partir do app/site. | https://developer.apple.com/app-store/review/guidelines/#user-generated-content | VERIFICADO |
| **G1.2 UGC** — responsabilidade de remover conteúdo violador | **Sim** | "It is your responsibility to remove content that violates this guideline, your terms of service, or your community standards." Descumprimento → plano de conformidade; conduta "egregious or repeated" → remoção do app e do Apple Developer Program. Implica ter Termos de Uso / community standards publicados. | https://developer.apple.com/app-store/review/guidelines/#user-generated-content | VERIFICADO |
| **G1.2 UGC** — as 4 salvaguardas em conjunto | **Sim** | Guideline 1.2 exige simultaneamente: (1) filtro pré-publicação; (2) denúncia com resposta tempestiva; (3) bloqueio de usuários; (4) contato publicado. É causa comum de rejeição de apps de comunidade. | https://developer.apple.com/app-store/review/guidelines/ | VERIFICADO |
| **G1.2.1 Creator Content** — controle de faixa etária | **Depende** | Apps de *creator content* devem "provide a way for users to identify content that exceeds the app's age rating, and use an age restriction mechanism based on verified or declared age". Posts comuns de membros **não** caracterizam creator app por si só; reavaliar se houver criadores destacados ou monetização de conteúdo. | https://developer.apple.com/app-store/review/guidelines/#user-generated-content | VERIFICADO |
| **G5.1.1(v)** — exclusão de conta dentro do app | **Sim** | Texto exato: "If your app doesn't include significant account-based features, let people use it without a login. If your app supports account creation, you must also offer account deletion within the app." | https://developer.apple.com/app-store/review/guidelines/#data-collection-and-storage | VERIFICADO |
| **G5.1.1(v)** — não exigir dados pessoais sem relação com a funcionalidade central | **Depende** | "Apps may not require users to enter personal information to function, except when directly relevant to the core functionality of the app or required by law." Features de comunidade baseadas em conta tornam o login defensável; conteúdo puramente de leitura idealmente acessível sem conta. | https://developer.apple.com/app-store/review/guidelines/#data-collection-and-storage | VERIFICADO |
| **Exclusão de conta** — apagar o registro inteiro; desativar não basta | **Sim** | "Offer to delete the entire account record, along with associated personal data. You may include additional options, but only offering to temporarily deactivate or disable an account is insufficient." Em vigor "Starting June 30, 2022". | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **Exclusão de conta** — deve incluir o UGC compartilhado com outros | **Sim** | "This includes user-generated content that's shared with others, such as photos, video, text posts, and reviews." Impacto direto: posts em canais e fotos precisam entrar no fluxo de deleção. | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **Exclusão de conta** — opção fácil de achar; link direto se concluir na web | **Sim** | "Make the account deletion option easy to find in your app. Typically, it's included in the app's account settings." Se o encerramento for na web, incluir link **direto para a página específica** — link genérico para a home não satisfaz. | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **Exclusão de conta** — exceção só para setores altamente regulados | **Não (não se aplica)** | Única exceção: apps em "highly regulated industries" (5.1.1(ix): bancos/financeiro, saúde, apostas, cannabis legal, aviação, exchanges de cripto) podem usar fluxos adicionais de atendimento. Os demais "should not require people to make a phone call, send an email, or go through other support flows". Lumen+ **não** se enquadra. | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **Exclusão de conta** — vale para todos os usuários, em qualquer país | **Sim** | "All users should be allowed to delete their accounts, regardless of where they're located." Contas criadas automaticamente também precisam de deleção; processo manual é aceitável desde que o prazo seja informado ao usuário. | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **Exclusão de conta** — criação via navegador não isenta a deleção in-app | **Sim** | FAQ oficial: se o app leva ao navegador para criar conta, ainda assim precisa oferecer deleção dentro do app — "Yes". Além disso, sair para o navegador padrão para login/registro "provides a poor user experience and is not appropriate, per App Store Review Guideline 4". | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **Exclusão de conta** — revogar tokens se usar Sign in with Apple | **Depende** | "Apps that support Sign in with Apple should use the Sign in with Apple REST API to revoke user tokens." Hoje o app usa Firebase Auth (Google/e-mail) → ainda não aplicável; torna-se obrigatório junto com a implementação de SiwA. | https://developer.apple.com/support/offering-account-deletion-in-your-app/ | VERIFICADO |
| **G4.8 Login Services** — Google Sign-In dispara a exigência de login alternativo | **Sim** | Apps que usam login social de terceiro (o texto cita "Facebook Login, Google Sign-In, Log in with X") para a conta primária "must also offer as an equivalent option another login service" que: (a) "limits data collection to the user's name and email address"; (b) "allows users to keep their email address private as part of setting up their account"; (c) "does not collect interactions with your app for advertising purposes without consent". **A guideline atual não nomeia "Sign in with Apple" no texto** — exige um serviço que atenda aos 3 critérios. | https://developer.apple.com/app-store/review/guidelines/#login-services | VERIFICADO |
| **G4.8** — as exceções não cobrem o Lumen+ | **Não (nenhuma exceção disponível)** | Exceções: (1) o app usa **exclusivamente** sistemas próprios de conta; (2) marketplace alternativo; (3) app educacional/corporativo que exige conta existente da instituição; (4) identificação cidadã/eID governamental; (5) cliente de um serviço terceiro específico. A exceção (1) exige **exclusividade** — como o Lumen+ também oferece Google Sign-In, o e-mail/senha próprio via Firebase **não** isenta. | https://developer.apple.com/app-store/review/guidelines/#login-services | VERIFICADO |
| **G5.1.2(i)** — permissão explícita via ATT | **Não (não há tracking)** | "You must receive explicit permission from users via the App Tracking Transparency APIs to track their activity." Sem tracking, a obrigação de exibir o prompt não incide. 5.1.2 continua exigindo divulgar claramente compartilhamento de dados pessoais com terceiros e obter permissão explícita antes. | https://developer.apple.com/app-store/review/guidelines/ | VERIFICADO |
| **Screenshots — iPhone**: 6.9" obrigatório (ou 6.5" como alternativa) | **Sim** | A linha 6.5" traz "Required if app runs on iPhone and screenshots for 6.9\" display aren't provided", e a nota: "If screenshots with the accepted sizes aren't provided, scaled screenshots for 6.9\" displays are used." Basta **um** conjunto de iPhone; os demais tamanhos são escalados. | https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications/ | VERIFICADO |
| **Screenshots — iPad**: 13" obrigatório se o app roda em iPad | **Sim** (ver §3: `supportsTablet: true`) | Linha oficial do iPad 13": "Required if app runs on iPad". | https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications/ | VERIFICADO |
| **Screenshots** — quantidade, formatos e transparência | **Sim** | "You can upload a minimum of one and a maximum of ten screenshots in .jpeg, .jpg, and .png formats" e "Images can't include alpha channels or transparencies." Se a UI é igual entre tamanhos/localizações, enviar só a maior resolução — o resto escala. | https://developer.apple.com/help/app-store-connect/manage-app-information/upload-app-previews-and-screenshots/ | VERIFICADO |
| **Screenshots** — dimensões exatas em pixels (6.9" e 13") | **Sim (mas não confirmado)** | Tabela renderizada dinamicamente; leituras conflitantes. **Não usar número algum daqui.** Ver §4, item A. | https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications/ | **NÃO VERIFICADO** |
| **SDK/Xcode mínimo para submissão hoje** | **Sim** | Bloco "Since April 28, 2026": "Apps uploaded to App Store Connect must be built with Xcode 26 or later using an SDK for iOS 26, iPadOS 26, tvOS 26, visionOS 26, or watchOS 26." **Já em vigor** — build com Xcode 16.x / SDK iOS 18 é rejeitado no upload. | https://developer.apple.com/news/upcoming-requirements/ | VERIFICADO |
| **Classificação etária** — sistema atualizado | **Sim** | Bloco "Since January 31, 2026": os ratings de todos os apps foram atualizados automaticamente para o novo sistema, refletidos em dispositivos com iOS 26 / iPadOS 26 / macOS Tahoe 26 / tvOS 26 / visionOS 26 / watchOS 26. Relevante para app com UGC — conferir a classificação resultante no App Store Connect. | https://developer.apple.com/news/upcoming-requirements/ | VERIFICADO |

---

## 2. Apple — Privacidade (App Privacy, Privacy Manifests, Required Reason APIs, ATT, SDKs)

| Requisito | Aplicável ao Lumen+? | O que a fonte diz | Fonte oficial (URL) | Estado |
|---|---|---|---|---|
| **App Privacy (nutrition labels)** — declarar coleta no App Store Connect | **Sim** | Declarar todos os dados coletados, salvo exceções de *optional disclosure*. Categorias: Contact Info; Health & Fitness; Financial Info; Location (Precise/Coarse); Sensitive Info; Contacts; **User Content** (Emails or Text Messages, Photos or Videos, Audio Data, Gameplay Content, Customer Support, Other User Content); Browsing History; Identifiers (User ID, Device ID); Purchases; Usage Data; Diagnostics; Surroundings; Body; Other Data Types. "Collect" = transmitir para fora do dispositivo e armazenar de forma legível além do necessário para atender à requisição imediata. | https://developer.apple.com/app-store/app-privacy-details/ | VERIFICADO |
| **App Privacy** — classificação em 3 níveis | **Sim** | *Data Used to Track You* / *Data Linked to You* / *Data Not Linked to You*. "Unless data is de-identified or anonymized before collection… it's considered linked to the user." Para o Lumen+: **Used to Track = Não**; **Linked to You = Sim** (Contact Info, User ID e User Content ficam vinculados à identidade por causa das contas). | https://developer.apple.com/app-store/app-privacy-details/ | VERIFICADO |
| **App Privacy** — declarar *User Content* (Photos or Videos) | **Sim** | Uso de `expo-image-picker` com `NSPhotoLibraryUsageDescription` / `NSCameraUsageDescription` (foto de perfil e comprovante de pagamento) + posts em canais → cai em "User Content" > "Photos or Videos" e "Other User Content". Não se enquadra na exceção de *optional disclosure*: é funcionalidade primária, não coleta infrequente/opcional. | https://developer.apple.com/app-store/app-privacy-details/ | VERIFICADO |
| **App Privacy** — declarar propósitos de uso de cada dado | **Sim** | Propósitos oficiais: Third-Party Advertising; Developer's Advertising or Marketing; Analytics; Product Personalization; App Functionality; Other Purposes. Esperado no Lumen+: **App Functionality** (autenticação, features, segurança) e **Analytics** se houver SDK de analytics/erro ativo no build iOS. | https://developer.apple.com/app-store/app-privacy-details/ | VERIFICADO |
| **App Privacy** — URL de política de privacidade pública | **Sim** | "Privacy Policy (Required): Publicly accessible privacy policy URL". "Privacy Choices" URL é opcional. As respostas podem ser atualizadas no App Store Connect **sem** submeter novo build. | https://developer.apple.com/app-store/app-privacy-details/ | VERIFICADO |
| **App Privacy** — responsabilidade pelas práticas dos SDKs de terceiros | **Sim** | "Third-Party Partners" = ferramentas de analytics, ad networks, SDKs de terceiros e vendors externos; o desenvolvedor deve declarar os dados que eles coletam. Relevante para `@sentry/react` e `@vercel/analytics` no `package.json`. | https://developer.apple.com/app-store/app-privacy-details/ | VERIFICADO |
| **Privacy Manifest (`PrivacyInfo.xcprivacy`)** — quando é obrigatório | **Sim** | O SDK de terceiros precisa incluir manifest se estiver na lista oficial de SDKs; caso contrário, se usar *required reason API*, coletar dados, permitir que o app colete dados, ou contatar *tracking domains*. Para o **app**, a obrigação efetiva vem via Required Reason API. Nome do arquivo obrigatório: `PrivacyInfo.xcprivacy`. | https://developer.apple.com/documentation/bundleresources/privacy-manifest-files | VERIFICADO |
| **Privacy Manifest** — prazo em vigor | **Sim** | Anúncio de 29/02/2024: a partir de **13/03/2024** a Apple envia e-mail de notificação se o upload usar APIs que exigem motivo sem o motivo declarado; a partir de **01/05/2024** é obrigatório incluir motivo aprovado para enviar app novo ou atualização ao App Store Connect. Nenhuma data posterior foi encontrada nas páginas oficiais consultadas. | https://developer.apple.com/news/?id=3d8a9yyh | VERIFICADO |
| **Privacy Manifest** — chaves do arquivo | **Sim** | Property list raiz; `NSPrivacyAccessedAPITypes` é array de dicts, cada entrada com `NSPrivacyAccessedAPIType` (categoria) + `NSPrivacyAccessedAPITypeReasons` (array de códigos). O manifest registra tipos de dados coletados (todas as plataformas) e required reasons APIs (iOS, iPadOS, tvOS, visionOS, watchOS). **Cada executável/dylib** que usa required reason API precisa do próprio manifest. | https://developer.apple.com/documentation/bundleresources/describing-use-of-required-reason-api | VERIFICADO |
| **Required Reason API** — `NSPrivacyAccessedAPICategoryFileTimestamp` | **Sim** | APIs: `getattrlist`, `getattrlistbulk`, `fgetattrlist`, `fstat`, `fstatat`, `lstat`, `getattrlistat`. Motivos: **DDA9.1** (exibir timestamps ao usuário; não pode sair do dispositivo); **C617.1** (metadados dentro do container do app / app group / CloudKit); **3B52.1** (arquivos com acesso explicitamente concedido, ex. document picker); **0A2A.1** (wrapper de SDK de terceiro — *somente* SDKs de terceiros podem declarar). Declarado por `react-native`, `@react-native-async-storage/async-storage`, `expo-application`, `expo-file-system` no `node_modules`. | https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitypereasons | VERIFICADO |
| **Required Reason API** — `NSPrivacyAccessedAPICategoryUserDefaults` | **Sim** | API: `UserDefaults`. Motivos: **CA92.1** (informação acessível apenas ao próprio app); **1C8F.1** (mesmo App Group); **C56D.1** (wrapper de SDK de terceiro — somente SDKs); **AC6B.1** (`com.apple.configuration.managed` / `com.apple.feedback.managed` via MDM). Declarado por `expo-constants` e `react-native` (CA92.1). | https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitypereasons | VERIFICADO |
| **Required Reason API** — `NSPrivacyAccessedAPICategoryDiskSpace` | **Sim** | APIs: `statfs`, `statvfs`, `fstatfs`, `fstatvfs`, `getattrlist`, `fgetattrlist`, `getattrlistat`. Motivos: **85F4.1** (exibir espaço em disco ao usuário); **E174.1** (checar espaço antes de gravar / apagar quando baixo, com comportamento observável); **7D9E.1** (bug report opcional submetido pelo usuário); **B728.1** (apps de pesquisa em saúde — não se aplica). Declarado por `expo-file-system` (E174.1 e 85F4.1). | https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitypereasons | VERIFICADO |
| **Required Reason API** — `NSPrivacyAccessedAPICategorySystemBootTime` | **Sim** | API: `mach_absolute_time()`. Motivos: **35F9.1** (medir tempo decorrido entre eventos no app / timers; não pode sair do dispositivo, exceto o tempo decorrido); **8FFB.1** (timestamps absolutos de eventos do app); **3D61.1** (bug report opcional). Declarado pelo podspec `boost` do React Native (35F9.1). | https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitypereasons | VERIFICADO |
| **Required Reason API** — `NSPrivacyAccessedAPICategoryActiveKeyboards` | **Não** | Motivos: **3EC4.1** (app de teclado customizado, sendo teclado systemwide a funcionalidade primária); **54BD.1** (UI customizada conforme teclados ativos). Lumen+ não é app de teclado e nenhuma dependência declara esta categoria. **Não declarar.** | https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitypereasons | VERIFICADO |
| **Required Reason API** — o motivo declarado limita o uso | **Sim** | "You may use these APIs and the data derived from their use for the declared reasons only." As APIs não podem ser usadas para fingerprinting nem tracking, independentemente de permissão. Novo motivo pode ser solicitado em developer.apple.com/contact/request/privacy-manifest-reason/. | https://developer.apple.com/documentation/bundleresources/describing-use-of-required-reason-api | VERIFICADO |
| **Lista oficial de SDKs** que exigem manifest + assinatura | **Depende** | Lista com 86 SDKs, incl. Abseil, AFNetworking, Alamofire, AppAuth, BoringSSL/openssl_grpc, Capacitor, Charts, Cordova, FBSDK*, **Firebase\*** (FirebaseAuth, FirebaseCore, FirebaseCrashlytics, FirebaseFirestore, FirebaseMessaging…), Flutter, GoogleSignIn, GoogleUtilities, **hermes**, Kingfisher, Lottie, nanopb, OneSignal*, OpenSSL, Protobuf, RealmSwift, RxSwift, SDWebImage, SnapKit, SwiftyJSON, UnityFramework. O requisito vale ao submeter app novo com esses SDKs ou update que os adicione; assinaturas exigidas quando usados como dependência binária; qualquer versão e qualquer repackaging estão incluídos. **A página não declara data-limite.** | https://developer.apple.com/support/third-party-SDK-requirements/ | VERIFICADO |
| **Signature requirement** — aplicação concreta: `hermes` | **Sim** | `hermes` consta na lista (item 45). O app usa `react-native` 0.76.9 sem chave `jsEngine` em `app.json` → Hermes é o motor padrão. A obrigação recai sobre o `hermes-engine` distribuído pelo React Native, não sobre código do Lumen+; satisfeita usando versões do RN que já embarcam manifest/assinatura — **sem ação de código no repositório**. | https://developer.apple.com/support/third-party-SDK-requirements/ | VERIFICADO |
| **Signature requirement** — as entradas `Firebase*` **não** se aplicam | **Não** | O `package.json` usa o pacote npm `firebase` ^10.7.1 = **Firebase JS SDK** (JavaScript puro); não há `.podspec` em `node_modules/firebase`, logo não traz os pods nativos FirebaseCore/FirebaseAuth/etc. A lista da Apple cobre os SDKs **nativos iOS**. Só passaria a valer com migração para `@react-native-firebase`. | https://developer.apple.com/support/third-party-SDK-requirements/ | VERIFICADO |
| **ATT (App Tracking Transparency)** — quando é exigido | **Não** | Exigido desde iOS/iPadOS/tvOS 14.5 para (a) rastrear usuários através de apps/sites de **outras** empresas e (b) acessar o IDFA. Lumen+ não tem tracking, ads nem IDFA → **não precisa exibir o prompt**. Sem permissão, o IDFA retorna zeros. | https://developer.apple.com/app-store/user-privacy-and-data-use/ | VERIFICADO |
| **ATT** — o que **não** conta como tracking | **Sim (base da resposta "No")** | Não é tracking: vincular dados de terceiros **somente no dispositivo** sem enviar informação identificável para fora; compartilhar com data brokers exclusivamente para detecção/prevenção de fraude ou segurança; compartilhar com agências de consumer reporting para avaliação de crédito. Sustenta a resposta *"Data Used to Track You: No"*. | https://developer.apple.com/app-store/user-privacy-and-data-use/ | VERIFICADO |
| **ATT** — proibição de fingerprinting mesmo sem prompt | **Sim** | Não é permitido fingerprinting nem usar sinais do dispositivo para identificar usuários sem permissão de ATT. O desenvolvedor responde por todos os SDKs de terceiros e pelo código do app, incluindo as práticas de tracking deles. Proibido bloquear funcionalidade ou incentivar o usuário a conceder a permissão. | https://developer.apple.com/app-store/user-privacy-and-data-use/ | VERIFICADO |

---

## 3. Google Play

| Requisito | Aplicável ao Lumen+? | O que a fonte diz | Fonte oficial (URL) | Estado |
|---|---|---|---|---|
| **UGC** — sistema in-app de denúncia e bloqueio | **Sim** | Apps com UGC devem ter moderação robusta incluindo "providing an in-app system for reporting and blocking objectionable UGC and users, and taking action against UGC". Apps sociais / com UGC público "must implement in-app functionality to report users and content, and to block users". | https://support.google.com/googleplay/android-developer/answer/9876937 | VERIFICADO |
| **UGC** — aceite de termos **antes** de criar ou enviar conteúdo | **Sim** | A moderação deve "Require users accept the app's terms of use and/or user policy before users can create or upload UGC". O aceite tem de ser **bloqueante no fluxo de criação de post**, não apenas no cadastro. | https://support.google.com/googleplay/android-developer/answer/9876937 | VERIFICADO |
| **UGC** — definir e proibir conteúdo/comportamento censurável nos termos | **Sim** | O app deve "Define objectionable content and behaviors (in a way that complies with Google Play Developer Program Policies), and prohibit them in the app's terms of use or user policies". | https://support.google.com/googleplay/android-developer/answer/9876937 | VERIFICADO |
| **UGC** — moderação efetiva (tomar ação) | **Sim** | Exige "taking action against UGC or users where appropriate". **A página não fixa SLA nem prazo de resposta**; exige a existência do mecanismo e da ação. | https://support.google.com/googleplay/android-developer/answer/9876937 | VERIFICADO |
| **UGC** — bloqueio in-app em interações 1:1 | **Depende** | "UGC features that enable 1:1 user interaction with specific users … must provide an in-app functionality for blocking users". Lumen+ hoje é canais (muitos-para-muitos). Se entrar DM/chat 1:1, o bloqueio in-app passa a ser exigido também por este dispositivo específico. | https://support.google.com/googleplay/android-developer/answer/9876937 | VERIFICADO |
| **UGC** — contato do desenvolvedor exigido pela política de UGC | **Não (não por esta política)** | A página de UGC **não** impõe publicação de canal de contato/e-mail nem prazo de resposta como requisito da política de UGC. Contato do desenvolvedor é exigido em outro contexto (ficha da loja) e um ponto de contato de segurança infantil é exigido pela Child Safety Standards Policy (linha abaixo). | https://support.google.com/googleplay/android-developer/answer/9876937 | VERIFICADO |
| **Child Safety Standards Policy** | **Depende da categoria declarada** | Escopo: "Anonymous and Random Chat apps, and apps in the Social and Dating categories". Exige: padrões publicados contra CSAE em recurso web globalmente acessível, funcional, com escopo relevante e citando o app ou o desenvolvedor; "Provide an in-app mechanism for user feedback" (experiência in-app, e-mail de suporte ou chat); endereçar CSAM; cumprir leis de segurança infantil; e fornecer "a name and contact information for an individual who is ready and able to speak to your organization's CSAM prevention practices". Declaração feita no Play Console. **Confirmar a categoria antes de decidir.** | https://support.google.com/googleplay/android-developer/answer/14747720 | VERIFICADO |
| **Exclusão de conta** — caminho **in-app** | **Sim** | Se o app permite criação de conta, é obrigatório "provide users with an in-app path to delete their app accounts and associated data". | https://support.google.com/googleplay/android-developer/answer/13327111 | VERIFICADO |
| **Exclusão de conta** — URL web pública, declarada no Play Console | **Sim** | Além do caminho in-app, obrigatório "provide a web link resource where users can request app account deletion and associated data deletion" — porque o usuário pode já ter desinstalado o app. O link vai "in the designated URL form field within Play Console", como parte do Data safety. **É "E", não "OU".** | https://support.google.com/googleplay/android-developer/answer/13327111 | VERIFICADO |
| **Exclusão de conta** — escopo dos dados e transparência sobre retenção | **Sim** | Excluir "the user data associated with that app account": dados pessoais, informações de autenticação, localização, dados de saúde e "all user data indicated as collected in your data safety section". Retenção parcial permitida apenas para "security, fraud prevention or regulatory compliance", e nesse caso é obrigatório "clearly inform users about your data retention practices, for example, within your privacy policy". | https://support.google.com/googleplay/android-developer/answer/13327111 | VERIFICADO |
| **Exclusão de conta** — isenções | **Não (nenhuma se aplica)** | "Permanently private and enterprise device management apps are exempt from this policy requirement." Apps não-móveis (Android TV, Wear OS, web) são isentos do caminho in-app mas continuam obrigados ao link web. Lumen+ é app móvel público. | https://support.google.com/googleplay/android-developer/answer/13327111 | VERIFICADO |
| **Target API level** — API 36 (Android 16) a partir de 31/08/2026 | **Sim** | A partir de **31/08/2026**, novos apps e atualizações devem ter como alvo "Android 16 (API level 36) or higher". Até 30/08/2026 vale a exigência anterior (Android 15 / API 35+). Apps existentes precisam de API 35+ para permanecerem disponíveis/descobríveis para novos usuários. Fora do requisito, o app fica indisponível para novos usuários em dispositivos com Android superior ao target. **Hoje é 2026-08-06 — restam ~25 dias.** | https://support.google.com/googleplay/android-developer/answer/11926878 | VERIFICADO |
| **Target API level** — extensão até 01/11/2026 | **Depende** | "Developers will be able to request an extension to November 1, 2026 if you need more time to update your app." O formulário fica na página *Policy status* do Play Console; apenas apps não conformes recebem o aviso e "The extension form is available through the details page of the warning or issue." | https://support.google.com/googleplay/android-developer/answer/11926878 | VERIFICADO |
| **Teste fechado obrigatório** — 12 testadores por 14 dias contínuos | **Depende do tipo/idade da conta** | Aplica a "Developers with personal accounts created after November 13, 2023": "you must run a closed test for your app with a minimum of 12 testers who have been opted-in for at least the last 14 days continuously". Os 14 dias devem ser **consecutivos** — entra-e-sai não conta. Só depois é possível solicitar acesso à produção. | https://support.google.com/googleplay/android-developer/answer/14151465 | VERIFICADO |
| **Data safety** — formulário obrigatório e suas seções | **Sim** | Seções: (1) Data collection and security — se coleta/compartilha, criptografia em trânsito e mecanismo de exclusão; (2) Data types; (3) Data usage and handling — finalidade e se a coleta é obrigatória ou opcional; (4) Security practices. "You alone are responsible for making complete and accurate declarations in your app's store listing on Google Play." | https://support.google.com/googleplay/android-developer/answer/10787469 | VERIFICADO |
| **Data safety** — declarar coleta feita por SDKs/bibliotecas de terceiros | **Sim** | A declaração "includes data collected and handled through any third-party libraries or SDKs used in their apps". "Collect" = transmitir dados para fora do dispositivo, incluindo dados vindos de bibliotecas, SDKs e webviews controlados pelo app. Auditar dependências antes de declarar. | https://support.google.com/googleplay/android-developer/answer/10787469 | VERIFICADO |
| **Data safety** — criptografia em trânsito e solicitação de exclusão | **Sim** | O formulário exige indicar se há "encryption in transit" e se os usuários podem "request deletion of their data", além de marcar cada tipo como *optional* ou *required*. Deve ser coerente com o caminho in-app + URL web de exclusão. | https://support.google.com/googleplay/android-developer/answer/10787469 | VERIFICADO |
| **Screenshots** — quantidade, dimensões e formato mínimos | **Sim** | Mínimo de **2** screenshots (somando todos os tipos de dispositivo) para publicar; até 8 por tipo. Dimensão mínima 320px, máxima 3840px, e "The maximum dimension of your screenshot can't be more than twice as long as the minimum dimension". Formato: "JPEG or 24-bit PNG (no alpha)". | https://support.google.com/googleplay/android-developer/answer/9866151 | VERIFICADO |
| **Screenshots** — critério de elegibilidade a recomendação/destaque | **Depende (não bloqueia publicação)** | Para ser elegível a recomendação: "at least four screenshots with minimum 1080px resolution. These should be 16:9 for landscape (minimum 1920x1080px) and 9:16 for portrait screenshots (minimum 1080x1920px)". | https://support.google.com/googleplay/android-developer/answer/9866151 | VERIFICADO |
| **Screenshots de tablet / Chromebook** | **Depende da distribuição** | "For Chromebook and tablets, you can add a minimum of 4 screenshots to demonstrate your in-app experience", entre 1.080 e 7.680px, 16:9 (landscape) e 9:16 (portrait). | https://support.google.com/googleplay/android-developer/answer/9866151 | VERIFICADO |
| **Feature graphic** — obrigatório para publicar | **Sim** | Feature graphic é obrigatório. Especificação: **1024px × 500px**, "JPEG or 24-bit PNG (no alpha)". | https://support.google.com/googleplay/android-developer/answer/9866151 | VERIFICADO |

---

## 4. Expo / EAS — SDK, Build, Submit, configuração

| Requisito | Aplicável ao Lumen+? | O que a fonte diz | Fonte oficial (URL) | Estado |
|---|---|---|---|---|
| **EAS Build** — imagens com Xcode 26.x disponíveis e selecionáveis | **Sim** | A tabela de infraestrutura lista imagens iOS com Xcode 26.x (ex.: `macos-tahoe-26.5-xcode-26.6` = alias `latest`; `macos-tahoe-26.4-xcode-26.4`; `macos-sequoia-15.6-xcode-26.2`; `macos-sequoia-15.6-xcode-26.0`) e imagens legadas com Xcode 16.4 associadas a SDKs antigos. **Sem `image` no `eas.json`, o build usa o alias `auto`**, que seleciona a imagem pela SDK do projeto — com Expo SDK 52 isso tende a cair numa imagem Xcode 16.x, que **não** atende ao requisito Apple em vigor. Política declarada: "We aim to support all stable Xcode releases that allow you to submit your app to the App Store Connect when used during the build process." | https://docs.expo.dev/build-reference/infrastructure/ | VERIFICADO |
| **Expo SDK 52 / RN 0.76 compila e submete com Xcode 26 / SDK iOS 26** | **Depende — risco de bloqueio** | Sem declaração oficial localizada. Ver §5, item C. **Este é o risco de submissão mais provável hoje.** | https://docs.expo.dev/build-reference/infrastructure/ | **NÃO VERIFICADO** |
| **Expo SDK atual (latest)** | **Sim** | A tabela de compatibilidade lista apenas SDK 54.0.0, 55.0.0, 56.0.0 e 57.0.0. `latest` = **SDK 57.0.0** (React Native 0.86, React 19.2.3). SDK 56 = RN 0.85; SDK 55 = RN 0.83; SDK 54 = RN 0.81. **SDK 52 não aparece em nenhuma tabela desta página.** | https://docs.expo.dev/versions/latest/ | VERIFICADO |
| **Política de suporte / vida útil de um SDK** | **Sim** | "SDK releases will continue to have a lifetime of approximately one year. We may revisit the details of this policy in the future." O exemplo dado na própria página refere-se ao SDK 54 (setembro/2025). A página **não** usa a palavra "deprecated" nem lista SDKs sem suporte. Data de release do SDK 57 informada: **30 de junho de 2026**. | https://expo.dev/changelog/sdk-57 | VERIFICADO |
| **Expo Go suporta apenas o SDK mais recente** | **Sim** | "Expo Go only supports the latest SDK version and previous versions are no longer supported." Para development builds, a compatibilidade retroativa nos serviços EAS é "much longer, but not forever". Recomendação oficial: atualizar SDK incrementalmente, um por vez. | https://docs.expo.dev/workflow/upgrading-expo-sdk-walkthrough/ | VERIFICADO |
| **Dados oficiais do SDK 52** | **Sim** | Changelog de **12/11/2024**: SDK 52 traz React Native 0.76; New Architecture habilitada por padrão em projetos novos (`newArchEnabled: true`); iOS deployment target 13.4 → **15.1**; Android `minSdkVersion` 23 → **24** e `compileSdkVersion` 34 → **35**. Posteriormente o SDK 52 passou a suportar também RN 0.77, com 0.76 como padrão. | https://expo.dev/changelog/2024-11-12-sdk-52 | VERIFICADO |
| **Docs versionados do SDK 52 ainda hospedados?** | **Não** | `https://docs.expo.dev/versions/v52.0.0/` e `.../sdk/notifications/` retornam **HTTP 404**; a URL equivalente do SDK atual (`v57.0.0`) responde normalmente. **É evidência de remoção da doc versionada — não é declaração textual de fim de suporte.** | https://docs.expo.dev/versions/v52.0.0/ | VERIFICADO |
| **Declaração oficial explícita de fim de suporte do SDK 52** | **Depende** | Nenhuma página oficial afirma textualmente que o SDK 52 não é suportado. Ver §5, item T. | https://docs.expo.dev/versions/latest/ | **NÃO VERIFICADO** |
| **EAS Build** — pré-requisitos gerais | **Sim** | (1) projeto React Native; (2) conta Expo (plano gratuito ou pago) em expo.dev; (3) EAS CLI instalado globalmente ou via `npx eas-cli@latest`; (4) autenticação com `eas login`. | https://docs.expo.dev/build/setup/ | VERIFICADO |
| **EAS Build (Android)** — conta de desenvolvedor Google Play | **Sim** | "Google Play Developer membership is required to distribute to the Google Play Store" (taxa única de **USD 25**). Também são necessárias credenciais de assinatura (keystore), geradas pelo EAS CLI, reaproveitadas ou fornecidas manualmente. | https://docs.expo.dev/build/setup/ | VERIFICADO |
| **EAS Build (iOS)** — Apple Developer Program | **Sim** | "Apple Developer Program membership is required to build for the Apple App Store" (**USD 99/ano**). Necessários provisioning profile e distribution certificate, gerados pelo EAS CLI ou fornecidos pelo desenvolvedor. | https://docs.expo.dev/build/setup/ | VERIFICADO |
| **EAS Build (Android)** — AAB é o padrão de produção | **Sim** | "The default file format used when building Android apps with EAS Build is an Android App Bundle (AAB/.aab)". AAB é otimizado para a Google Play e **não** pode ser instalado diretamente em dispositivos. Gera-se APK só quando `android.buildType = "apk"`, `distribution = "internal"`, `developmentClient = true`, ou `android.gradleCommand` customizado. | https://docs.expo.dev/build-reference/apk/ | VERIFICADO |
| **Credenciais de assinatura Android (keystore)** | **Sim** | "Google requires all Android apps to be digitally signed with a certificate before they are installed on a device or updated." O EAS pode gerar e gerenciar o keystore. Com Play App Signing, se o upload keystore for perdido/comprometido, é possível acionar o suporte do Google Play para redefinir a chave. | https://docs.expo.dev/app-signing/app-credentials/ | VERIFICADO |
| **Credenciais iOS** — distribution certificate, provisioning profile, push key | **Sim** | Distribution certificate: "You may only have one distribution certificate associated with your Apple Developer account." Provisioning profile: "Each profile is app-specific… for every app you submit to the App Store" e expira em 12 meses (regenerável sem afetar apps em produção). Push key: "A single key can be used with any number of apps", máximo de 2 por conta. | https://docs.expo.dev/app-signing/app-credentials/ | VERIFICADO |
| **Build de produção Android (AAB) — passos** | **Sim** | Pré-requisitos: (1) conta paga Google Play Developer; (2) perfil `production` em `eas.json` (incluído por padrão); (3) opcionalmente e-mail + JSON key de Google Service Account para automatizar o release. Comando: `eas build --platform android` (`production` é o perfil padrão). Saída `.aab`, distribuível somente via Google Play. | https://docs.expo.dev/tutorial/eas/android-production-build/ | VERIFICADO |
| **Build de produção iOS — passos** | **Sim** | Pré-requisitos: (1) conta Apple Developer; (2) perfil `production` em `eas.json`. Fluxo: `eas credentials` para estabelecer o distribution provisioning profile de produção, depois `eas build --platform ios` e `eas submit --platform ios`. | https://docs.expo.dev/tutorial/eas/ios-production-build/ | VERIFICADO |
| **`eas.json` e perfis de build** | **Sim** | Gerado na primeira execução de `eas build:configure`. Default traz `development` (`developmentClient: true`, `distribution: "internal"`), `preview` (`distribution: "internal"`) e `production` (`{}`). Builds de produção "are submitted to an app store, for release to the general public or as part of a store-facilitated testing process such as TestFlight". | https://docs.expo.dev/build/eas-json/ | VERIFICADO |
| **EAS Submit (Android)** — pré-requisitos | **Sim** | "A Google Play Developer account is required to submit your app to the Google Play Store" e "EAS requires a Google Service Account key to submit on your behalf." Necessários: JSON key da service account nas credenciais do EAS; `android.package` em `app.json`; build de produção `.aab`; app já existente no Google Play Console. A **primeira submissão vai por padrão para a track de internal testing**; a store listing precisa estar completa antes de promover para produção. Em CI/CD, usar `EXPO_TOKEN`. | https://docs.expo.dev/submit/android/ | VERIFICADO |
| **EAS Submit (iOS)** — pré-requisitos | **Sim** | "A paid Apple Developer account is required to submit apps to the Apple App Store." Além disso: bundle identifier em `app.json`; EAS CLI instalado e logado; build `.ipa` de produção via EAS Build, corretamente assinado. | https://docs.expo.dev/submit/ios/ | VERIFICADO |
| **EAS Submit (iOS)** — autenticação na App Store Connect | **Sim** | Dois métodos: (1) **App Store Connect API Key** — criada pelo gerenciamento de credenciais do Expo, ou fornecida em `eas.json` via `ascApiKeyPath`, `ascApiKeyIssuerId`, `ascApiKeyId`; (2) **app-specific password** — na variável `EXPO_APPLE_APP_SPECIFIC_PASSWORD` com o Apple ID no campo `appleId`. | https://docs.expo.dev/submit/ios/ | VERIFICADO |
| **EAS Submit (iOS)** — `ascAppId` | **Sim** | O perfil de submit iOS precisa do `ascAppId`. Onde encontrar: listado no App Store Connect sob *General Information* como "Apple ID" (App Store Connect → time → Apps → app → aba App Store → General > App Information). Após o comando, o build aparece no TestFlight tipicamente em 10–15 minutos. | https://docs.expo.dev/submit/eas-json/ | VERIFICADO |
| **EAS Submit (iOS)** — `appleTeamId` é obrigatório? | **Depende** | Não listado como obrigatório na página de submit iOS. Ver §5, item Y. | https://docs.expo.dev/submit/ios/ | **NÃO VERIFICADO** |
| **`PrivacyInfo.xcprivacy`** — o que é e onde fica | **Sim** | "A privacy manifest is a file named PrivacyInfo.xcprivacy that is included in your iOS native project." | https://docs.expo.dev/guides/apple-privacy/ | VERIFICADO |
| **Campo `expo.ios.privacyManifests`** | **Sim** | "You can include an iOS privacy manifest by using the privacyManifests field under expo.ios in your app config." Estrutura: `NSPrivacyAccessedAPITypes`, cada entrada com `NSPrivacyAccessedAPIType` (ex.: `NSPrivacyAccessedAPICategoryUserDefaults`) e `NSPrivacyAccessedAPITypeReasons` (ex.: `["CA92.1"]`). | https://docs.expo.dev/guides/apple-privacy/ | VERIFICADO |
| **Pacotes do Expo SDK já trazem `PrivacyInfo` próprio** | **Sim** | "All Expo SDK packages that use required reason APIs file have a PrivacyInfo file included in the package directory." **Porém**: "Apple does not correctly parse all the PrivacyInfo files included by static CocoaPods dependencies (such as Expo SDK packages and other ecosystem libraries)" → o desenvolvedor precisa consolidar manualmente as declarações no app config. | https://docs.expo.dev/guides/apple-privacy/ | VERIFICADO |
| **Consolidar os motivos das dependências no manifest do app** | **Sim** | Auditoria dos 9 `PrivacyInfo.xcprivacy` em `lumen_mobile/node_modules`: FileTimestamp **C617.1** (react-native, ReactCommon/cxxreact, boost, glog, RCT-Folly, async-storage, expo-application); FileTimestamp **0A2A.1** e **3B52.1** (expo-file-system); UserDefaults **CA92.1** (expo-constants, react-native); DiskSpace **E174.1** e **85F4.1** (expo-file-system); SystemBootTime **35F9.1** (boost). Conjunto sugerido para o app: FileTimestamp `[C617.1, 3B52.1]`, UserDefaults `[CA92.1]`, DiskSpace `[E174.1, 85F4.1]`, SystemBootTime `[35F9.1]`. **Não declarar `0A2A.1` no manifest do app** — a Apple restringe esse motivo a SDKs de terceiros. | https://docs.expo.dev/guides/apple-privacy/ | VERIFICADO |
| **Fluxo recomendado de configuração do manifest** | **Sim** | (1) `npx expo install --fix`; (2) identificar as required reasons nos `PrivacyInfo.xcprivacy` das libs em `node_modules`; (3) adicionar as declarações em `ios.privacyManifests` no `app.json`; (4) validar submetendo. | https://docs.expo.dev/guides/apple-privacy/ | VERIFICADO |
| **Validar o manifest via TestFlight / App Store review** | **Sim** | "Apple will email you within a few minutes of submitting if your app is missing any required reasons." É o método oficial de verificação, coerente com o anúncio da Apple de 29/02/2024. | https://docs.expo.dev/guides/apple-privacy/ | VERIFICADO |
| **Prebuild/CNG gera o `PrivacyInfo.xcprivacy` automaticamente?** | **Depende** | A página não afirma onde o arquivo é gerado nem se o prebuild o cria a partir de `ios.privacyManifests`. Ver §5, item W. | https://docs.expo.dev/guides/apple-privacy/ | **NÃO VERIFICADO** |
| **`NSPrivacyTracking` e `NSPrivacyCollectedDataTypes` via app config** | **Depende** | Essas chaves **não** aparecem na página oficial do Expo sobre privacy manifests. Ver §5, item X. | https://docs.expo.dev/guides/apple-privacy/ | **NÃO VERIFICADO** |
| **expo-notifications** — requisitos básicos de push | **Sim, se/quando houver push nativo** | Necessários: (1) permissão do usuário; (2) `ExpoPushToken`; (3) libs `expo-notifications` e `expo-constants`; (4) config plugin `expo-notifications` no app config; (5) `projectId`. O guia usa EAS Build como caminho recomendado. | https://docs.expo.dev/push-notifications/push-notifications-setup/ | VERIFICADO |
| **expo-notifications (Android)** — credenciais FCM V1 | **Sim, se houver push nativo** | Duas credenciais distintas: (1) **FCM V1 service account key (JSON)** — Firebase Console → Project settings → Service accounts; sensível, deve ir para `.gitignore`; enviada via `eas credentials` ou dashboard EAS. (2) **`google-services.json`** — baixado do Firebase Console, na raiz do projeto, referenciado por `android.googleServicesFile`. Se a API key do `google-services.json` estiver restrita, é preciso liberar a **FCM Registration API** e a **Firebase Installations API** no Google Cloud Console, senão o app não recebe push token. | https://docs.expo.dev/push-notifications/fcm-credentials/ | VERIFICADO |
| **expo-notifications (iOS)** — APNs / push key | **Sim, se houver push nativo** | Exige conta Apple Developer paga; device de teste registrado previamente; responder "sim" quando o primeiro build EAS perguntar se deseja habilitar push (o EAS gera a APNs key). Uma única push key serve para qualquer número de apps, máximo de 2 por conta. | https://docs.expo.dev/push-notifications/push-notifications-setup/ | VERIFICADO |
| **expo-notifications** — limitação no Expo Go | **Sim** | "Push notifications (remote notifications) functionality provided by expo-notifications is unavailable in Expo Go on Android from SDK 53. A development build is required to use push notifications." "Local notifications (in-app notifications) remain available in Expo Go." No Android é preciso criar *notification channels* **antes** de obter o push token. | https://docs.expo.dev/versions/latest/sdk/notifications/ | VERIFICADO |
| **`app.json`** — `name`, `slug`, `version` | **Sim** | `name` (Required) — "The name of your app as it appears both within Expo Go and on your home screen as a standalone app"; `slug` (Required) — "A URL-friendly name for your project that is unique across your account"; `version` (Required) — mapeia para `CFBundleShortVersionString` (iOS) e `versionName` (Android). | https://docs.expo.dev/versions/latest/config/app/ | VERIFICADO |
| **`app.json` (iOS)** — `ios.bundleIdentifier` e `ios.buildNumber` | **Sim** | `ios.bundleIdentifier` é obrigatório para builds iOS — identificador único em notação DNS reversa. `ios.buildNumber` corresponde a `CFBundleVersion` e deve seguir o formato exigido pela Apple. | https://docs.expo.dev/versions/latest/config/app/ | VERIFICADO |
| **`app.json` (Android)** — `android.package` e `android.versionCode` | **Sim** | `android.package` é obrigatório para builds Android — nome de pacote único na Google Play, em notação DNS reversa. `android.versionCode` — "Version number required by Google Play. Increment by one for each release. Must be a positive integer." | https://docs.expo.dev/versions/latest/config/app/ | VERIFICADO |
| **`app.json`** — `owner` | **Depende** | "The name of the Expo account that owns the project… If not provided, the owner defaults to the username of the current user." **Não é obrigatório**, mas é necessário na prática quando o projeto pertence a uma organização/time e não ao usuário logado. | https://docs.expo.dev/versions/latest/config/app/ | VERIFICADO |
| **`app.json`** — `extra.eas.projectId` | **Sim** | "When eas init runs, it associates a unique identifier for our project in app.json under extra.eas.projectId" e "The value of this property is used to identify our project on EAS servers." `eas update:configure` também adiciona o campo. A página `config/app` descreve `extra` genericamente ("Any extra fields you want to pass to your experience", acessível via `Constants.expoConfig.extra`) e **não** documenta especificamente `extra.eas.projectId`. | https://docs.expo.dev/tutorial/eas/configure-development-build/ | VERIFICADO |
| **`app.json`** — `runtimeVersion` | **Depende** | Indica compatibilidade entre código nativo e atualizações OTA; aceita string ou objeto de policy. **Não** listado como obrigatório para build de loja — relevante apenas se o projeto usar EAS Update. | https://docs.expo.dev/versions/latest/config/app/ | VERIFICADO |

---

## 5. Aplicação ao Lumen+

Base factual: leitura do repositório em `main` (`7db785d` + working tree), 2026-08-06.
Arquivos citados são absolutos a partir de `C:/Users/Elias/Documents/Projeto Lumen+/lumenplus-main/`.

> **Aviso sobre doc anterior:** `docs/store-readiness/account-deletion-evidence.md` afirma que a tela
> in-app de exclusão e a página web pública "NÃO EXISTEM". **Isso está desatualizado** — os commits
> `58d464b` e `57cb813` adicionaram `lumen_mobile/app/account/delete.tsx` e
> `lumen_mobile/app/excluir-conta.tsx`. Aquele documento precisa ser revisado.

### 5.1 UGC — Apple G1.2 e Google Play UGC

| Sub-requisito | JÁ ATENDE | FALTA |
|---|---|---|
| Filtro de material censurável **antes** da publicação (Apple) | — | **Nada encontrado.** `backend/app/api/channel_routes.py` cria posts (`POST /{org_unit_id}/posts`, linha 279) e respostas (linha 377) sem qualquer filtro; não há serviço de moderação automática em `backend/app/services/` (apenas `account_deletion`, `audit_service`, `inbox_service`, `legal_cache`, `organization`, `role_service`). **Blocker Apple G1.2.** |
| Mecanismo de denúncia (backend) | `backend/app/api/moderation_routes.py`: `POST /reports` (linha 168), `GET /reports` (230), `PATCH /reports/{report_id}` (286) — com limite `MAX_OPEN_REPORTS_PER_USER` e idempotência por alvo. | Nada no backend. |
| Mecanismo de denúncia (**UI no app**) | — | **Nenhuma UI encontrada.** Busca por `report`/`denunciar`/`moderation` em `lumen_mobile/app/` e `lumen_mobile/src/` só retorna nomes de estilo (`destTextBlock`). O endpoint existe mas **não é alcançável pelo usuário**. **Blocker Apple G1.2 e Google Play UGC.** |
| Bloqueio de usuário (backend) | `moderation_routes.py`: `POST /blocks` (338), `DELETE /blocks/{user_id}` (371), `GET /blocks` (392). | — |
| Bloqueio de usuário (**UI no app**) | — | **Nenhuma UI encontrada.** Mesmo diagnóstico acima. **Blocker nas duas lojas.** |
| Resposta tempestiva a denúncias (processo operacional) | Fluxo de triagem existe tecnicamente (`PATCH /reports/{id}` + `GET /reports`). | **Processo documentado + responsável nomeado + evidência para o App Review.** Apple exige "timely responses"; a guideline não publica SLA (ver §6, item E). |
| Aceite de termos **antes** de criar/enviar UGC (Google Play) | Termos de Uso existem em `backend/app/legal_content.py`; há registro de `UserConsent`. | **Verificar/implementar o gate bloqueante no fluxo de criação de post** — a política exige aceite antes de criar ou enviar UGC, não apenas no cadastro. |
| Definir e proibir conteúdo censurável nos termos (Google Play) | Termos de Uso publicados (`legal_content.py`). | **Confirmar que os Termos definem explicitamente "conteúdo censurável" e comportamentos proibidos**, alinhados às Developer Program Policies. Não confirmei essa cláusula nesta leitura. |
| Contato publicado (Apple G1.2) | `legal_content.py:85` traz e-mail de contato (`juridico@obralumen.org.br`); a página pública de exclusão usa `lgpd@lumenserfeliz.org`. | **Support URL na ficha da App Store** + **unificar o e-mail de contato**: o comentário de versão em `legal_content.py:247` indica migração de `juridico@obralumen.org.br` para `comunicacao@lumenserfeliz.org`, e há um terceiro endereço (`lgpd@lumenserfeliz.org`) em uso. Três endereços diferentes em circulação é risco de inconsistência no review. |
| Responsabilidade de remover conteúdo violador | `DELETE /{org_unit_id}/posts/{post_id}` (324) e `DELETE …/replies/{reply_id}` (423) existem. | Community standards publicados e vinculados ao fluxo de moderação. |
| Bloqueio 1:1 (Google Play) | N/A hoje — o modelo é canais (muitos-para-muitos). | Reavaliar se DM/chat 1:1 entrar no produto. |
| G1.2.1 Creator Content (Apple) | N/A — não há criadores destacados nem monetização de conteúdo. | Reavaliar se isso mudar. |

### 5.2 Exclusão de conta

| Sub-requisito | JÁ ATENDE | FALTA |
|---|---|---|
| Endpoint de auto-exclusão | `DELETE /auth/me` em `backend/app/api/routes/auth.py:311`. | — |
| Caminho **in-app** (Apple 5.1.1(v) + Google Play) | `lumen_mobile/app/account/delete.tsx` chama `api.delete('/auth/me')` (linha 49); acessível a partir de `lumen_mobile/app/(tabs)/profile.tsx:632`. Atende à exigência de estar "easy to find… in the app's account settings". | — |
| **Apagar o registro inteiro** (Apple) | — | **Não atende.** `backend/app/services/account_deletion.py::anonymize_user` **anonimiza**: apaga `UserProfile`, `UserPreferences`, `OrgMembership`, `UserGlobalRole` e anonimiza o e-mail em `UserIdentity`, mas **mantém a linha `User` com `is_active=False`**, além de `UserConsent` e `AuditLog`. A Apple diz que oferecer apenas desativar/desabilitar "is insufficient" e exige apagar "the entire account record". **Conflito direto entre a estratégia de retenção legal (LGPD/auditoria, 5 anos) e a regra da Apple — precisa de decisão jurídica + técnica antes da submissão.** |
| **Excluir o UGC compartilhado com outros** (Apple) | — | **Não atende.** `anonymize_user` não toca posts de canal, respostas, fotos enviadas (perfil/comprovante) nem Projeto de Vida. A Apple é explícita: inclui "photos, video, text posts, and reviews". **Blocker.** |
| Link web público de solicitação (Google Play) | `lumen_mobile/app/excluir-conta.tsx` — página pública, sem login, que orienta o caminho autenticado e oferece canal do Encarregado (`lgpd@lumenserfeliz.org`) para quem perdeu o acesso. A política do Google fala em "request app account deletion", o que essa página cumpre. | **Declarar a URL no campo designado do Play Console** (parte do Data safety) e confirmar que a URL está publicamente acessível no domínio final. |
| Não exigir e-mail/telefone (Apple) | O caminho in-app é direto e não passa por suporte — conforme. | Manter assim. A página web com `mailto` é aceitável porque é o canal **adicional** exigido pelo Google, não o caminho in-app. |
| Escopo dos dados + transparência de retenção (Google Play) | Retenção declarada na Política de Privacidade. | **Divergência conhecida:** o doc interno `account-deletion-evidence.md` registra política dizendo 30 dias / 2 anos e código dizendo 5 anos. O Google permite retenção parcial só para "security, fraud prevention or regulatory compliance" **e** exige informar claramente as práticas. **Reconciliar política ↔ código.** |
| Revogar tokens Sign in with Apple | N/A hoje (Firebase Auth: Google + e-mail/senha). | Torna-se obrigatório junto com a implementação de SiwA (ver 5.3). |
| Todos os países / contas automáticas | Fluxo é único e global. | — |

### 5.3 Login (Apple G4.8)

- **JÁ ATENDE:** nada que satisfaça a G4.8 hoje.
- **FALTA:** o app usa Firebase Auth com **Google Sign-In** como login primário → a G4.8 **se aplica**.
  A exceção de "sistemas próprios" exige **exclusividade**, e o e-mail/senha do Firebase convive com o
  Google Sign-In, logo **não isenta**. É preciso oferecer outro serviço de login que atenda aos 3
  critérios (dados limitados a nome e e-mail; e-mail privado na criação da conta; sem coleta de
  interações para publicidade sem consentimento). **A guideline atual não nomeia "Sign in with Apple"**
  — e eu **não** consegui confirmar em fonte oficial que o SiwA atende formalmente aos 3 critérios
  (ver §6, item B). Consequência em cadeia: adotar SiwA aciona a obrigação de revogar tokens na exclusão de conta.

### 5.4 Privacidade Apple (App Privacy + Privacy Manifest + ATT)

| Sub-requisito | JÁ ATENDE | FALTA |
|---|---|---|
| App Privacy — nutrition labels | — | **Preencher no App Store Connect.** Esperado: Contact Info, Identifiers (User ID), **User Content (Photos or Videos + Other User Content)** → todos **Linked to You**; propósito **App Functionality**. |
| App Privacy — "Data Used to Track You" | Base factual sólida: sem ads, sem IDFA, sem data brokers. | Responder **No**. |
| App Privacy — URL de política pública | Política de privacidade existe em `backend/app/legal_content.py`. | **Publicar numa URL pública estável** e informá-la no App Store Connect. |
| App Privacy — SDKs de terceiros | — | Auditar `@sentry/react` e `@vercel/analytics`. Observação factual: o pacote instalado é `@sentry/react` (SDK **web**), não `@sentry/react-native`. **Não confirmei se eles executam no build nativo iOS** (ver §6, item L) — sem isso, não dá para declarar Diagnostics/Usage Data com segurança. |
| Privacy Manifest (`ios.privacyManifests`) | — | **`lumen_mobile/app.json` não tem a chave `ios.privacyManifests`** (confirmado por leitura). O projeto usa CNG (não há pasta `ios/`), então o manifest do app só sai dessa config. Conjunto sugerido (§4): FileTimestamp `[C617.1, 3B52.1]`, UserDefaults `[CA92.1]`, DiskSpace `[E174.1, 85F4.1]`, SystemBootTime `[35F9.1]`. **Nunca declarar `0A2A.1` no app.** |
| Signature requirement — `hermes` | `react-native` 0.76.9, sem `jsEngine` em `app.json` → Hermes padrão; a obrigação recai sobre o `hermes-engine` do RN. | Nada no repositório. **Mas** não confirmei que o hermes-engine do RN 0.76.9 já traz manifest+assinatura válidos (§6, item J). |
| Signature requirement — `Firebase*` | Não aciona: `firebase` ^10.7.1 é o **JS SDK**, sem `.podspec` em `node_modules/firebase`. | Reavaliar se migrar para `@react-native-firebase`. |
| ATT | Não aplicável (sem tracking/ads/IDFA). | Nada a implementar. Garantir que nenhum SDK futuro faça fingerprinting. |

### 5.5 Toolchain e build

| Sub-requisito | JÁ ATENDE | FALTA |
|---|---|---|
| **Xcode 26 / SDK iOS 26 (obrigatório desde 28/04/2026)** | — | **`lumen_mobile/eas.json` não define `image` em nenhum perfil** → alias `auto`, que escolhe a imagem pela SDK do projeto. Com Expo SDK 52 isso tende a cair em Xcode 16.x → **upload rejeitado**. **Este é o risco de bloqueio nº 1.** E não há confirmação oficial de que SDK 52 / RN 0.76 compila com Xcode 26 / SDK iOS 26 (§6, item C) — **provável necessidade de upgrade de SDK antes de submeter.** |
| Expo SDK em suporte | — | `package.json`: `expo ~52.0.0`, `react-native 0.76.9`. SDK 52 é de 12/11/2024 e **não aparece** nas tabelas de compatibilidade atuais (que listam 54–57); docs versionadas em 404. Política de vida útil ≈ 1 ano. **Fortemente indicado que está fora de suporte, mas sem declaração textual oficial (§6, item T).** |
| `app.json` — campos base | `name: "Lumen+"`, `slug: "lumen-plus"`, `version: "1.0.0"`, `ios.bundleIdentifier` e `android.package` = `com.lumenchristi.lumenplus`. | — |
| `app.json` — `owner` e `extra.eas.projectId` | — | **Ambos ausentes.** Sem `extra.eas.projectId`, o EAS não resolve o projeto. Blocker humano já catalogado em `docs/store-readiness/human-blockers.md` (A1). |
| `ios.buildNumber` / `android.versionCode` | Mitigado: `eas.json` usa `cli.appVersionSource: "remote"` e `build.production.autoIncrement: true`. | Confirmar que o versionamento remoto está inicializado no EAS antes do primeiro release. |
| Formato de build Android | `eas.json` → `build.production.android.buildType: "app-bundle"` (AAB). Correto para a Google Play. | — |
| EAS Submit | Perfil `submit.production` existe. | **`appleId`, `ascAppId`, `appleTeamId` e `serviceAccountKeyPath` estão vazios** (com comentários "PENDENTE HUMANO" no próprio `eas.json`). Preferir App Store Connect API Key à senha específica. |
| Contas de desenvolvedor | — | Apple Developer Program (USD 99/ano) e Google Play Developer (USD 25) — blockers humanos A2 e A3 em `human-blockers.md`. |
| **Target API 36 (prazo 31/08/2026)** | — | **Não verifiquei qual `targetSdkVersion` o Expo SDK 52 gera** (§6, item O). Faltam ~25 dias. Se não atingir API 36, é preciso a extensão até 01/11/2026 pelo Play Console (só disponível para apps já com aviso de política). **Um upgrade de SDK provavelmente resolve os dois problemas (Xcode 26 e API 36) de uma vez.** |
| Push notifications | Backend tem `push_routes.py` (Web Push). | `package.json` **não tem `expo-notifications`** e `app.json` não declara o plugin → os requisitos de FCM V1 / APNs **não incidem no build nativo atual**. Se push nativo entrar no escopo, todos os requisitos da §4 passam a valer. |

### 5.6 Assets e ficha de loja

| Sub-requisito | JÁ ATENDE | FALTA |
|---|---|---|
| Ícones | `store-assets/apple/icon-1024.png`; `store-assets/google-play/icon-512.png`. | — |
| Feature graphic (Google Play, obrigatório) | `store-assets/google-play/feature-graphic-1024x500.png` — nome bate com a spec 1024×500. | Confirmar formato sem canal alpha (JPEG ou PNG 24-bit). |
| **Screenshots iPhone (obrigatório)** | — | **Nenhum screenshot no repositório** (`store-assets/` só tem ícones + feature graphic + `social-preview-1200x630.png`). É preciso 1 conjunto de iPhone 6.9" (ou 6.5"), 1 a 10 imagens, sem alpha. **Dimensões exatas em px não confirmadas (§6, item A).** |
| **Screenshots iPad 13" (obrigatório)** | — | **`lumen_mobile/app.json` tem `ios.supportsTablet: true`** → o app roda em iPad → **screenshots de iPad 13" são obrigatórios**. Alternativa: mudar para `supportsTablet: false` (decisão de produto, não de conformidade). |
| Screenshots Google Play | — | Mínimo de 2 para publicar; recomendado 4 com 1080px+ em 9:16 para elegibilidade a recomendação. Nenhum existe hoje. |
| Metadados | `store-metadata/apple/pt-BR.md` e `store-metadata/google-play/pt-BR.md`. | Support URL (Apple), URL de exclusão de conta (Play Console), política de privacidade pública. |
| Permissões declaradas | `app.json`: `NSPhotoLibraryUsageDescription` e `NSCameraUsageDescription` com textos específicos; Android `READ_MEDIA_IMAGES` + `CAMERA`; `blockedPermissions` remove `RECORD_AUDIO`, localização e `SYSTEM_ALERT_WINDOW`. Boa higiene. | **Não consultei a política de Photo and Video Permissions do Google Play** (§6, item S) — verificar antes do envio, já que `READ_MEDIA_IMAGES` é declarada. |

### 5.7 Google Play — processo

| Sub-requisito | JÁ ATENDE | FALTA |
|---|---|---|
| Data safety form | — | Preencher as 4 seções, declarando também a coleta por SDKs de terceiros, criptografia em trânsito e possibilidade de solicitar exclusão — coerente com o caminho in-app + URL web. |
| Teste fechado (12 testadores / 14 dias contínuos) | — | **Depende de dado interno**: só se aplica a contas **pessoais** criadas após 13/11/2023 (§6, item P). Se aplicar, são **14 dias corridos antes** de solicitar produção — impacta o cronograma. |
| Child Safety Standards | — | **Depende da categoria declarada** (§6, item Q). Se o Lumen+ for publicado como **Social**, os 4 requisitos passam a valer (padrões CSAE em URL global, feedback in-app, tratamento de CSAM, contato nomeado). |

### 5.8 Os 5 bloqueios mais prováveis para submeter hoje

1. **iOS não compila conforme**: sem `image` no `eas.json` e com Expo SDK 52, o build tende a sair com Xcode 16.x — rejeitado no upload desde 28/04/2026.
2. **UGC sem filtro pré-publicação e sem UI de denúncia/bloqueio** — os endpoints existem, a interface não. Rejeição quase certa na G1.2 (Apple) e na política de UGC (Google Play).
3. **Exclusão de conta anonimiza em vez de apagar, e não remove o UGC** — conflito frontal com a regra da Apple.
4. **`app.json` sem `extra.eas.projectId`/`owner` e `eas.json` de submit vazio** — nem chega a existir build/submissão.
5. **Zero screenshots** para as duas lojas (e iPad 13" obrigatório por causa de `supportsTablet: true`).

---

## 6. NÃO VERIFICADO

> **Nenhuma conclusão de conformidade pode ser tirada dos itens abaixo.** São lacunas onde a fonte
> oficial estava inacessível, renderizada por JavaScript, retornou 404, ou onde o dado depende de
> informação interna que não está no repositório. Tratar cada item como **pendência de verificação
> humana**, não como requisito.

**A.** **Apple — dimensões exatas em pixels dos screenshots 6.9" (iPhone) e 13" (iPad).** A tabela do App Store Connect Help é renderizada dinamicamente; as extrações retornaram valores conflitantes para o 6.9" (1320×2868 numa leitura, 1260×2736 noutra) e leituras não confirmadas para o iPad 13" (2064×2752 / 2048×2732 retrato). **Não usar nenhum desses números.** Abrir manualmente https://developer.apple.com/help/app-store-connect/reference/app-information/screenshot-specifications/ antes de gerar os assets. *(A regra de **quais** tamanhos são obrigatórios está VERIFICADA — só os pixels não estão.)*

**B.** **Apple — se o Sign in with Apple atende formalmente aos 3 critérios da G4.8.** As páginas `developer.apple.com/sign-in-with-apple/` e a de Human Interface Guidelines retornaram apenas o título (conteúdo renderizado por JavaScript). Está verificado que a **G4.8 se aplica** ao Lumen+ e **quais** são os 3 critérios; **não** está verificado que o SiwA os satisfaz.

**C.** **Expo — compatibilidade do Expo SDK 52 / React Native 0.76 com Xcode 26 e SDK iOS 26.** Nenhuma declaração oficial localizada; a tabela de imagens EAS não associa alias explícito `sdk-52` a imagem Xcode 26.x. **Risco alto de bloqueio de submissão**, dado que a Apple exige Xcode 26 + SDK iOS 26 desde 28/04/2026.

**D.** **Apple — prazo/carência do App Review para apps já publicados que ainda não oferecem exclusão in-app.** Não localizei texto oficial atual; a página de suporte só declara a vigência desde 30/06/2022 para apps submetidos.

**E.** **Apple — o que conta como "timely responses" na G1.2 (SLA de moderação).** Não há número oficial publicado na guideline.

**F.** **Apple — requisitos de classificação etária específicos para apps com UGC no novo sistema.** Não consultei a página do questionário do App Store Connect; apenas o aviso de atualização automática de ratings foi verificado.

**G.** **Expo — versão mínima do SDK que suporta `expo.ios.privacyManifests`.** A página oficial não declara versão mínima e afirma que o recurso "is still in development and may change". Não verificado se o Expo SDK 52 exige plugin adicional ou config extra.

**H.** **Apple — data-limite do requisito de assinatura (signature) de SDKs de terceiros.** A página https://developer.apple.com/support/third-party-SDK-requirements/ **não declara data alguma**. O prazo de 01/05/2024 foi verificado apenas para *Required Reason APIs*, no anúncio https://developer.apple.com/news/?id=3d8a9yyh.

**I.** **Apple — data da última revisão da lista de 86 SDKs.** A página não exibe data de atualização. A lista foi lida em 2026-08-06; não verificado se houve inclusões posteriores não refletidas.

**J.** **Apple / React Native — se o `hermes-engine` distribuído com `react-native` 0.76.9 já vem com privacy manifest e assinatura válidos.** Não verificado em fonte oficial Apple nem em reactnative.dev; não foi encontrado `PrivacyInfo.xcprivacy` do hermes em `node_modules` (o pod é baixado como binário pré-compilado no prebuild / `pod install`).

**K.** **Apple — páginas do App Store Connect Help sobre o fluxo de preenchimento das App Privacy questions.** Não acessadas nesta consulta. As categorias foram confirmadas apenas em https://developer.apple.com/app-store/app-privacy-details/.

**L.** **Repo/Apple — se `@sentry/react` e `@vercel/analytics` chegam a executar no build nativo iOS** e, portanto, o que exatamente precisa ser declarado em Diagnostics / Usage Data. Observação factual do repositório: o pacote instalado é `@sentry/react` (SDK **web**), não `@sentry/react-native`. **Requer confirmação do time** sobre o alvo real (web via react-native-web vs. nativo).

**M.** **Apple — existência de requisito posterior a 01/05/2024 sobre privacy manifests** (ex.: novas categorias de Required Reason API adicionadas em 2025/2026). As 5 categorias e 17 códigos foram lidos na documentação oficial em 2026-08-06, mas **não** foi feita varredura do histórico completo de `developer.apple.com/news`.

**N.** **Google — fonte oficial inacessível:** a página do Play Console Help "Complete the Data safety form" (https://support.google.com/googleplay/android-developer/answer/11317317) retornou **HTTP 404**. A lista literal dos nomes de campo do formulário no Play Console — incluindo o **rótulo exato do campo de URL de exclusão de conta** — não foi lida em página oficial. As seções listadas vieram de https://support.google.com/googleplay/android-developer/answer/10787469.

**O.** **Expo/Android — qual `targetSdkVersion` o Expo SDK 52 / RN 0.76 gera por padrão, e se atinge API 36.** Não consultado. **Não afirmar número algum.** Verificar em docs.expo.dev (changelog do SDK) e no build real (`android/app/build.gradle` ou `expo-build-properties`) antes de assumir conformidade com o prazo de 31/08/2026.

**P.** **Google — se a conta de desenvolvedor do Lumen+ é PESSOAL e foi criada após 13/11/2023.** Depende de dado interno. Sem isso não é possível afirmar se a regra de 12 testadores / 14 dias contínuos se aplica. Verificar em Play Console → Configurações da conta de desenvolvedor.

**Q.** **Google — a categoria que o Lumen+ declarará na Play Store.** A Child Safety Standards Policy só alcança apps de chat anônimo/aleatório e as categorias **Social** e **Dating**. Se for publicado como Social, os quatro requisitos passam a ser obrigatórios com declaração no Play Console.

**R.** **Google — prazo/SLA de resposta a denúncias.** A página oficial de UGC consultada **não** traz prazo nem exigência de e-mail de contato público como parte da política de UGC. **Não afirmo prazo algum.** Se um SLA for necessário, ele não vem desta fonte.

**S.** **Google — política de permissões de foto e vídeo (`READ_MEDIA_IMAGES` / Photo and Video Permissions policy).** Não consultada, embora o Lumen+ declare `READ_MEDIA_IMAGES` e `CAMERA`. Verificar separadamente em support.google.com/googleplay/android-developer antes do envio.

**T.** **Expo — declaração textual oficial de que o SDK 52 está "deprecated" / "unsupported".** Nenhuma página oficial afirma isso literalmente. O que existe: política de vida útil ≈ 1 ano (expo.dev/changelog/sdk-57), ausência do SDK 52 nas tabelas de compatibilidade de docs.expo.dev/versions/latest (que listam apenas 54–57), e docs versionadas em HTTP 404. **Tratar como "fortemente indicado", não como citação oficial.**

**U.** **Expo — página dedicada de "SDK support policy": fonte oficial inacessível.** https://expo.dev/changelog/sdk-support-policy retorna **HTTP 404**. A política só foi encontrada como parágrafo dentro do changelog do SDK 57.

**V.** **Expo — data exata de fim das correções críticas do SDK 52.** O changelog do SDK 57 exemplifica a política com o SDK 54 (setembro/2025), não com o 52. **Não afirmar prazo específico para o 52.**

**W.** **Expo — se o prebuild/CNG gera automaticamente o `PrivacyInfo.xcprivacy` a partir de `ios.privacyManifests`.** docs.expo.dev/guides/apple-privacy não faz essa afirmação. Não inventar comportamento.

**X.** **Expo — suporte a `NSPrivacyTracking` e `NSPrivacyCollectedDataTypes` via app config.** Essas chaves não aparecem na página oficial do Expo sobre privacy manifests, que trata apenas de `NSPrivacyAccessedAPITypes`. A declaração de tracking e de tipos de dados coletados pertence ao domínio da Apple (App Privacy no App Store Connect) e não foi verificada em fonte Apple nesta consulta.

**Y.** **Expo — `appleTeamId` é obrigatório em `eas.json` para EAS Submit iOS?** Não listado como obrigatório em docs.expo.dev/submit/ios. O `eas.json` do projeto já reserva o campo. Confirmar em docs.expo.dev/submit/eas-json antes de tratar como obrigatório.

**Z.** **Expo — mensagem de erro exata do EAS CLI quando `extra.eas.projectId` está ausente.** Não lida em página oficial; apenas o fato de que `eas init` e `eas update:configure` adicionam o campo foi verificado.

**AA.** **Expo — versão mínima de `eas-cli` exigida hoje.** O `eas.json` do projeto declara `cli.version ">= 10.0.0"`, mas nenhuma página oficial confirmando a versão atual/mínima do `eas-cli` em 2026-08-06 foi lida.

### Notas de escopo (não são lacunas de fonte)

- **`supportsTablet`**: constava como não verificado em consulta anterior. **Resolvido por leitura do repositório**, não por fonte oficial: `lumen_mobile/app.json` tem `"supportsTablet": true` → screenshots de iPad 13" são obrigatórios.
- Duas ressalvas de escopo das consultas parciais originais ("esta resposta cobre apenas Google Play" / "o escopo pedido foi restrito a docs.expo.dev") **estão supridas neste documento consolidado**, que cobre Apple, Google Play e Expo. Não são pendências.

---

## 7. Como manter este documento

- Reverificar antes de cada submissão: as guidelines da Apple e as políticas do Google Play mudam sem aviso.
- Ao verificar qualquer item da §6, mover a linha para a tabela da plataforma correspondente com a URL e a **nova data de consulta** — nunca reaproveitar a data 2026-08-06.
- Nunca promover um item de §6 para VERIFICADO sem ter lido a página oficial. Fonte secundária não conta.
